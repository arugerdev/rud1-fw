# /etc/modules-load.d/rud1.conf
# Kernel modules required by rud1-agent on boot.

# USB/IP (remote USB device attach over TCP)
usbip-core
usbip-host
vhci-hcd

# WireGuard is compiled into the RPi OS kernel; no module needed on Bookworm+.
